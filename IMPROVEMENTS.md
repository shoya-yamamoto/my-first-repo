# 上海ゲームの現状分析と改善

## 元の仕様・画面・構造
- 36種類×4枚＝144枚。亀・城・十字・ピラミッド・蜘蛛の5配置からランダムに開始。
- 同じ柄2枚を除去。上に重なりがなく左右のどちらかが空いた牌だけ選択可能。春・夏も同柄どうし。
- 連続成功で10×コンボ数を加点。違う柄の選択・配り直しでコンボをリセット。
- 上部に配置名・スコア・残り枚数、中央に盤面、下部にヒント・配り直し・全体表示・新規。終了時はモーダル。
- ピンチ拡大・タッチ移動・ホイール拡大に対応していたが、回転時の再計算やドラッグ後の誤タップ抑制が不十分。
- `index.html`内にReact/CDN/Babel・ルール・表示が同居。`shanghai.jsx`は別実装で、READMEの案内も公開HTMLと一致していなかった。
- 消去を遅延処理していたため、素早い連続操作や新規・配り直しとの競合が起こりうる。
- 初期配牌と配り直し後の合法手を保証せず、判定も次の1段しか見ていなかった。

## 優先順位と今回の実装
|優先度|観点|対応|
|---|---|---|
|P0|安定性|ペア除去と加点をreducerで同時確定。演出から状態更新を分離|
|P0|ルール|すべての上段の重なりを判定。手詰まり・クリアを現在の盤面から計算|
|P0|継続しやすさ|開始時・配り直し時に合法ペアを確保（自由な位置が2つ以上ある場合）|
|P1|遊びやすさ|一手戻す、取れるペア数、画面内の遊び方、取れない理由、配置選択|
|P1|見た目|深緑と象牙色の牌を維持。選択・ヒント・取れない牌を区別し、進捗バーを追加|
|P1|ゲームフィール|消去トランジション、即時加点表示、クリア表示、動きを減らす設定に対応|
|P1|操作・モバイル|Pointer Eventsでマウス・タッチを統一。ドラッグ後のクリック抑制、拡大縮小ボタン、ResizeObserverによる再フィット|
|P1|アクセシビリティ|牌をbutton化、柄と段の読み上げ、選択状態、キーボード操作、ネイティブdialogのフォーカス管理|
|P1|設定・視認性|牌の表面・角・側面・影を一体のSVGに変更。選択可能牌の強調、配り直し、一手戻すをオプション画面から個別に切替|
|P1|牌の重なり|下の牌の支えが足りない位置を各配置で修正。城の塔は下段から組み直し、支えのある半マスずらしは維持|
|P1|保守性|ルール・状態・React画面・CSSを分割。旧importパスは再export。依存を固定してローカルへバンドル|

## 確認結果
- `npm test`: 7テスト成功。全5配置×20回の配牌、144枚・各柄4枚、合法手、上段と左右の判定、コンボ、undo、100回の再配牌で牌集合保持、幾何的手詰まり、新規初期化を検証。さらに、全配置で上段の牌が直下に75%以上支えられ、同段の牌が重ならないことを検証。
- `npm run build`: 成功。配布用JavaScriptとCSSを生成。
- ブラウザーで画像・盤面表示、ペア消去（144→142枚・10点）、undo（144枚・0点）、遊び方ダイアログを確認。
- 390×844と844×390で画面幅と操作ボタンの収まりをDOM寸法で確認。実機のマルチタッチ操作は未検証。
- オプションの設定値はブラウザーに保存。設定画面と、SVG牌144枚の読み込み・厚み・重なりをブラウザーで確認。

## 次に取り組む改善
1. **P2: 解ける配牌** — 逆順配置またはソルバーでクリア可能な初期状態を生成。現在は「次の一手」があることだけを保証。
2. **P2: 再開機能** — localStorageによる途中保存と最高スコアの保存。
3. **P3: 演出** — 任意の効果音・触覚フィードバック、コンボ演出を実機で調整。

自由な位置が2つ未満なら柄だけの配り直しでは解決できません。その場合は一手戻すか新しいゲームを使います。5種類の輪郭とランダム配牌の性質は維持しています。
